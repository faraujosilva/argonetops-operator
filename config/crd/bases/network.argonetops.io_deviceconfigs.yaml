---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: deviceconfigs.network.argonetops.io
spec:
  group: network.argonetops.io
  names:
    kind: DeviceConfig
    listKind: DeviceConfigList
    plural: deviceconfigs
    singular: deviceconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceConfig is the Schema for the deviceconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeviceConfigSpec defines the desired state of DeviceConfig.
            properties:
              access:
                type: string
              deviceType:
                type: string
              hostname:
                type: string
              ip:
                type: string
              password:
                description: Password allows referencing a ConfigMap or Secret for
                  the password
                properties:
                  configMapKeyRef:
                    description: ConfigMapKeyRef references a key in a ConfigMap
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  secretKeyRef:
                    description: SecretKeyRef references a key in a Secret
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
              port:
                type: integer
              username:
                description: Username allows referencing a ConfigMap or Secret for
                  the username
                properties:
                  configMapKeyRef:
                    description: ConfigMapKeyRef references a key in a ConfigMap
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  secretKeyRef:
                    description: SecretKeyRef references a key in a Secret
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
            required:
            - access
            - deviceType
            - hostname
            - ip
            - port
            type: object
          status:
            description: DeviceConfigStatus defines the observed state of DeviceConfig.
            properties:
              message:
                type: string
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
